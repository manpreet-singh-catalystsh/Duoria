import * as React from "react"
import "./style.scss"
import "../../styles/index.scss"
export const Button = text => {
  return <button className="button1">{text}</button> //black button
}
export const Button2 = text => {
  return <button className="button2">{text}</button> //golden button
}
